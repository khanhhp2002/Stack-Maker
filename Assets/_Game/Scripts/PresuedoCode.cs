using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PresuedoCode : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        /* if(C_Brick.Color == S_Brick.Color)
             C_Brick.Count > 0 ? DeloyBrick() : (BlockMoveUp);
         else
             C_Brick.Count > 0 ? (!BlockMoveUp) : (BlockMoveUp);   */
    }
}
